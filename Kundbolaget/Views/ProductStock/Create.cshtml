@model Kundbolaget.ViewModels.ProductStockVM


@{
    ViewBag.Title = "Create";
}


<div class="row">
    @using (Html.BeginForm("Create", "ProductStock", FormMethod.Post, new {@class = "col s12"}))
    {
        @Html.AntiForgeryToken()
        <div class="form-horizontal">
            <h4>ProductStock</h4>
            <hr/>
            @Html.ValidationSummary(true)
            <div class="row">
                <div class="input-field col s3">
                    <div>
                        @Html.EditorFor(model => model.ProductStock.Amount, new
                        {
                            htmlAttributes = new
                            {
                                @class = "validate",
                                type = "number",
                                min = "0",
                                data_val_required = "Antal måste minst vara 0"
                            }
                        })
                        @Html.ValidationMessageFor(model => model.ProductStock.Amount)
                        @Html.LabelFor(model => model.ProductStock.Amount, "Antal")
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col s10">
                    @Html.LabelFor(model => model.ProductStock.ProductInfoId, "Produktartikel")
                    @Html.DropDownListFor(model => model.ProductStock.ProductInfoId, new SelectList(Model.ProductInfos , "Id", "Name"), new
                    {
                        htmlAttributes = new
                        {
                            @class = "dropdown-content"
                        }
                    })
                    @Html.ValidationMessageFor(model => model.ProductStock.ProductInfoId)
                </div>
            </div>

            <div class="row">
                <div class="col s10">
                    @Html.LabelFor(model => model.ProductStock.WarehouseId, "Varuhus")
                    @Html.DropDownListFor(model => model.ProductStock.WarehouseId, new SelectList(Model.Warehouses, "Id" , "Name"), new
                    {
                        htmlAttributes = new
                        {
                            @class = "dropdown-content"
                        }
                    })
                    @Html.ValidationMessageFor(model => model.ProductStock.WarehouseId)
                </div>
            </div>
            <div class="row">
                <button class="btn waves-effect waves-light" type="submit", name="action">
                    Skapa
                    <i class="material-icons right">add</i>
                </button>
            </div>
        </div>
    }

    <div>
        @Html.ActionLink("Backa", "Index")
    </div>
</div>
@*Is required for validation*@
<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script src="~/Scripts/jquery.validate.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>
@*Is required for validation*@